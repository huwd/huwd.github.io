<!doctype html>
<html lang="<%= site.locale %>">
  <%= render "head", metadata: site.metadata, title: data.title, tag: data.tag %>
  <body>
    <main role="main">
      <%= render Shared::Navbar.new(metadata: site.metadata, resource: resource, navigation: site.data.navigation) %>

      <%= yield %>
    </main>
  </body>
</html>
